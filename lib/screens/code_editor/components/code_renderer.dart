import 'package:flutter/material.dart';
import 'package:flutter_spark/models/models.dart';

class CodeRenderer extends StatelessWidget {
  final FSWidget widget;

  const CodeRenderer({Key? key, required this.widget}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
